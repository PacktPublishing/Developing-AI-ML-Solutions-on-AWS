[pytest]
# The SageMaker Feature Store shim lives in local/sagemaker-local and the ELT and
# lake scripts under src/; put both on the path so tests import them as top-level
# modules — no sys.path juggling in the test files.
pythonpath = src local/sagemaker-local
