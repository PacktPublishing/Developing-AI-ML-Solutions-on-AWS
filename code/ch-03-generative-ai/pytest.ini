[pytest]
# Shared seam (models.py, stores.py, corpus_data.py) and the element packages
# live under src/; put them on the path declaratively so tests import them as
# top-level modules — no sys.path juggling in the test files.
pythonpath = src src/underwriting-agent src/knowledge-base
