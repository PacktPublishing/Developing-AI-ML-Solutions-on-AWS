[pytest]
# The shared seam (models.py, stores.py) lives under src/ and the corpus steps
# under etl/; put both on the path declaratively so tests import them as
# top-level modules — no sys.path juggling in the test files.
pythonpath = src etl
